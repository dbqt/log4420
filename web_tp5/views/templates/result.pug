extends ../layout

block content
    script(src='/scripts/jquery.min.js')
    script(src='/scripts/result_logic.js')

    h1 Résultat
    
    section
        if(giveup)
            h2 Examen abandonné
            p L'examen a été abandonné.
        else
            h2 Examen complété
            
        p#resultat
        //script.
            generate_result();

        a.flatButton(href='/dashboard') Retour au tableau de bord
