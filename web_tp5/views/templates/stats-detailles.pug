div.modalBox.modalInvisible(*ngIf="!isHidden", (click)="hide()")
    div.modalBox
        div.modalBoxMessage
            h2 Statistiques détaillées
            table
                thead
                    tr
                        th Nom de l'examen
                        th Domaine
                        th Note obtenue
                tbody
                    tr(*ngFor="let row of stat")
                        td {{row.nom}}
                        td {{row.domaine}}
                        td {{row.score}}/{{row.nbQuestions}}
                        
                    

//-tr(*ngFor="let row of stat"){{row}})
//-<div *ngFor="let stat of stats">
//-    <h2>Stats detailles de {{stat.examen}}</h2>
//-    <div><label>score: </label>{{stat.score}}</div>
//-  </div>
//-<div (click)="hide()" class="modal fade" tabindex="-1" [ngClass]="{'in': visibleAnimate}"
//-       [ngStyle]="{'display': visible ? 'block' : 'none', 'opacity': visibleAnimate ? 1 : 0}">
